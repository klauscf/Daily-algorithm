/*******************************************
*	排序去重思路 
*	思路一：先将编号去重，然后排序输出 
*	思路二：先从小到大，输出时去重复 	 
********************************************/

思路二：
  先排序，冒泡，快速排序都可以，输出去重复代码思路
  
  printf("%d",a[1]);//输出第一个数
  for(i=2;1<=n;i++)//从2循环到n
  {
    if(a[i] != a[i-1])//如果这个数是第一次出现则输出
      printf("%d ",a[i]);
  }
